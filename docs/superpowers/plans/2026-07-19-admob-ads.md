# AdMob 広告実装 Implementation Plan（Issue #6）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ホーム下部バナー・ゲーム退出時インタースティシャル・王様ゲーム限定パック解放リワードの3広告を、プレミアム時は完全非表示の分岐つきで実装する。

**Architecture:** 広告ロジックは `src/lib/ads.ts` に集約し、UI（ホーム / GameScreen / PremiumPackModal）は薄い呼び出しのみ。SDK はルート `__mocks__/` の手動モックで全テストから自動的に切り離す。セッション解放は `pack-unlock-store`（メモリのみ）で管理する。

**Tech Stack:** react-native-google-mobile-ads（Expo config plugin）/ expo-tracking-transparency / jest-expo

**Spec:** `docs/superpowers/specs/2026-07-19-admob-ads-design.md`

## Global Constraints

- フォーマット: タブインデント・セミコロンなし・シングルクォート（Prettier 設定準拠、コミット前に `npx prettier --check` 対象ファイル）
- TDD 必須: 各タスクは RED（失敗ログ確認）→ GREEN（パス確認）→ commit。テストを実装に合わせて書き換えない
- テスト禁止事項: `any` / `.skip` / `.only` / 実タイマー / `Date.now()` 依存
- jest 実行: sandbox では `npx jest` が Console Ninja hook で落ちるため `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace <path>` を使う
- Expo Go では広告 SDK は動かない（動作確認は dev build。ユニットテストはモックで完結）
- インタースティシャル頻度: `INTERSTITIAL_EVERY_N_EXITS = 1`（毎回）。定数変更のみで頻度調整できる形を維持
- プレミアム分岐の唯一の判定源は `src/lib/premium.ts` の `isPremiumUnlocked()`
- 広告ユニット ID: 開発中は `TestIds`、本番 ID は AdMob コンソール発行後に `src/constants/ads.ts` を差し替え（リリース前チェックリスト）
- **SDK モックの明示（Task 2 実装時の知見）**: jest-expo ではルート `__mocks__/` が node_modules モジュールに自動適用されない。SDK に触るテスト（直接 import またはテスト対象が import）には `jest.mock('react-native-google-mobile-ads')` と必要に応じ `jest.mock('expo-tracking-transparency')` をファクトリなしで明示すること（ルートの手動モックが使われ、テストとテスト対象が同一インスタンスを共有する）。本番コードにテスト環境分岐を入れるのは禁止

---

### Task 1: SDK 導入・config plugin・テスト用手動モック

**Files:**

- Modify: `package.json`（依存追加は expo install 経由）
- Modify: `app.json`（plugins に広告 SDK を追加）
- Create: `__mocks__/react-native-google-mobile-ads.ts`
- Create: `__mocks__/expo-tracking-transparency.ts`

**Interfaces:**

- Produces: 全テストで SDK が自動モックされる基盤。モックの型は後続タスクのテストが `import type * as AdsMock from '../../../__mocks__/react-native-google-mobile-ads'` 形式で参照する

- [ ] **Step 1: パッケージ導入**

```bash
npx expo install react-native-google-mobile-ads expo-tracking-transparency
```

- [ ] **Step 2: app.json の plugins に追記**

`expo-audio` エントリの後に追加（App ID は Google 公式のテスト用。本番 ID はリリース前に差し替え）:

```json
[
	"react-native-google-mobile-ads",
	{
		"androidAppId": "ca-app-pub-3940256099942544~3347511713",
		"iosAppId": "ca-app-pub-3940256099942544~1458002511",
		"userTrackingUsageDescription": "広告の最適化のために端末の識別子を使用します。"
	}
]
```

- [ ] **Step 3: SDK の手動モックを作成**

`__mocks__/react-native-google-mobile-ads.ts`（ルート直下。node_modules のモジュールはこの場所で全テストに自動適用される）:

```ts
type Listener = (payload?: unknown) => void

export type MockAd = {
	addAdEventListener: jest.Mock
	load: jest.Mock
	show: jest.Mock
	_emit: (type: string, payload?: unknown) => void
}

function makeAd(): MockAd {
	const own = new Map<string, Set<Listener>>()
	return {
		addAdEventListener: jest.fn((type: string, fn: Listener) => {
			if (!own.has(type)) own.set(type, new Set())
			own.get(type)?.add(fn)
			return () => own.get(type)?.delete(fn)
		}),
		load: jest.fn(),
		show: jest.fn(),
		_emit(type: string, payload?: unknown) {
			own.get(type)?.forEach((fn) => fn(payload))
		},
	}
}

export const _interstitials: MockAd[] = []
export const _rewardeds: MockAd[] = []

export const _initialize = jest.fn(async () => [])
const mobileAds = jest.fn(() => ({ initialize: _initialize }))
export default mobileAds

export const TestIds = {
	ADAPTIVE_BANNER: 'test-adaptive-banner',
	INTERSTITIAL: 'test-interstitial',
	REWARDED: 'test-rewarded',
}

export const AdEventType = { LOADED: 'loaded', CLOSED: 'closed', ERROR: 'error' }
export const RewardedAdEventType = {
	LOADED: 'rewarded_loaded',
	EARNED_REWARD: 'rewarded_earned_reward',
}
export const BannerAdSize = { ANCHORED_ADAPTIVE_BANNER: 'ANCHORED_ADAPTIVE_BANNER' }

export const InterstitialAd = {
	createForAdRequest: jest.fn(() => {
		const ad = makeAd()
		_interstitials.push(ad)
		return ad
	}),
}

export const RewardedAd = {
	createForAdRequest: jest.fn(() => {
		const ad = makeAd()
		_rewardeds.push(ad)
		return ad
	}),
}

export const BannerAd = () => null

export const useRewardedAd = jest.fn(() => ({
	isLoaded: false,
	isEarnedReward: false,
	error: undefined,
	load: jest.fn(),
	show: jest.fn(),
}))

export const useForeground = jest.fn()

export function _resetAdsMock() {
	_interstitials.length = 0
	_rewardeds.length = 0
	_initialize.mockClear()
	InterstitialAd.createForAdRequest.mockClear()
	RewardedAd.createForAdRequest.mockClear()
}
```

- [ ] **Step 4: ATT の手動モックを作成**

`__mocks__/expo-tracking-transparency.ts`:

```ts
export const PermissionStatus = {
	UNDETERMINED: 'undetermined',
	GRANTED: 'granted',
	DENIED: 'denied',
}

function response(status: string) {
	return { status, granted: status === 'granted', canAskAgain: true, expires: 'never' as const }
}

export const getTrackingPermissionsAsync = jest.fn(async () =>
	response(PermissionStatus.UNDETERMINED),
)

export const requestTrackingPermissionsAsync = jest.fn(async () =>
	response(PermissionStatus.GRANTED),
)
```

- [ ] **Step 5: 既存テストにリグレッションがないことを確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace`
Expected: 全スイートパス（793 件以上）

- [ ] **Step 6: config plugin が効いていることを確認**

Run: `npx expo config --json 2>/dev/null | grep -A2 google-mobile-ads`
Expected: androidAppId / iosAppId が出力される

- [ ] **Step 7: Commit**

```bash
git add package.json bun.lock app.json __mocks__
git commit -m "feat(#6): 広告 SDK・ATT を導入しテスト用手動モックを整備"
```

---

### Task 2: 広告定数と初期化（ATT → AdMob init）・プレミアム分岐

**Files:**

- Create: `src/constants/ads.ts`
- Create: `src/lib/ads.ts`
- Test: `src/lib/__tests__/ads.test.ts`

**Interfaces:**

- Consumes: Task 1 のモック、`isPremiumUnlocked(): boolean`（src/lib/premium.ts）
- Produces: `adsEnabled(): boolean` / `initAds(): Promise<void>` / `maybeShowGameExitInterstitial(): void`（Task 3 で実装、ここでは初期化まで）/ `_resetForTest(): void` / 定数 `AD_UNIT_IDS`・`INTERSTITIAL_EVERY_N_EXITS`

- [ ] **Step 1: 失敗するテストを書く**

`src/lib/__tests__/ads.test.ts`:

```ts
import type * as AdsMock from '../../../__mocks__/react-native-google-mobile-ads'
import {
	getTrackingPermissionsAsync,
	PermissionStatus,
	requestTrackingPermissionsAsync,
} from 'expo-tracking-transparency'
import { adsEnabled, initAds, _resetForTest } from '@/lib/ads'
import { isPremiumUnlocked } from '@/lib/premium'

jest.mock('@/lib/premium', () => ({ isPremiumUnlocked: jest.fn(() => false) }))

const adsMock = jest.requireMock('react-native-google-mobile-ads') as typeof AdsMock
const mockedPremium = jest.mocked(isPremiumUnlocked)
const mockedGetAtt = jest.mocked(getTrackingPermissionsAsync)
const mockedRequestAtt = jest.mocked(requestTrackingPermissionsAsync)

function attResponse(status: PermissionStatus) {
	return {
		status,
		granted: status === PermissionStatus.GRANTED,
		canAskAgain: true,
		expires: 'never' as const,
	}
}

beforeEach(() => {
	jest.clearAllMocks()
	adsMock._resetAdsMock()
	_resetForTest()
	mockedPremium.mockReturnValue(false)
	mockedGetAtt.mockResolvedValue(attResponse(PermissionStatus.UNDETERMINED))
})

describe('adsEnabled', () => {
	it('プレミアムなら false、非プレミアムなら true', () => {
		expect(adsEnabled()).toBe(true)
		mockedPremium.mockReturnValue(true)
		expect(adsEnabled()).toBe(false)
	})
})

describe('initAds', () => {
	it('ATT 未回答なら許可リクエスト → AdMob 初期化の順で実行する', async () => {
		await initAds()
		expect(mockedRequestAtt).toHaveBeenCalledTimes(1)
		expect(adsMock._initialize).toHaveBeenCalledTimes(1)
		const attOrder = mockedRequestAtt.mock.invocationCallOrder[0]
		const initOrder = adsMock._initialize.mock.invocationCallOrder[0]
		expect(attOrder).toBeLessThan(initOrder)
	})

	it('ATT 回答済みなら許可リクエストを出さない', async () => {
		mockedGetAtt.mockResolvedValue(attResponse(PermissionStatus.GRANTED))
		await initAds()
		expect(mockedRequestAtt).not.toHaveBeenCalled()
		expect(adsMock._initialize).toHaveBeenCalledTimes(1)
	})

	it('プレミアムなら何もしない', async () => {
		mockedPremium.mockReturnValue(true)
		await initAds()
		expect(mockedGetAtt).not.toHaveBeenCalled()
		expect(adsMock._initialize).not.toHaveBeenCalled()
	})

	it('2回呼んでも初期化は1回だけ', async () => {
		await initAds()
		await initAds()
		expect(adsMock._initialize).toHaveBeenCalledTimes(1)
	})

	it('初期化後にインタースティシャルを先読みする', async () => {
		await initAds()
		expect(adsMock.InterstitialAd.createForAdRequest).toHaveBeenCalledTimes(1)
		expect(adsMock._interstitials[0]?.load).toHaveBeenCalledTimes(1)
	})
})
```

- [ ] **Step 2: RED を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/lib/__tests__/ads.test.ts`
Expected: FAIL（`@/lib/ads` が存在しない）

- [ ] **Step 3: 定数と実装を書く**

`src/constants/ads.ts`:

```ts
import { TestIds } from 'react-native-google-mobile-ads'

// インタースティシャルを何回のゲーム退出ごとに出すか（1 = 毎回）。
// 頻度を緩める場合はこの定数だけ変える（将来 Supabase 経由で上書き可能にする想定）
export const INTERSTITIAL_EVERY_N_EXITS = 1

// 本番ユニット ID は AdMob コンソール発行後に差し替える（リリース前チェックリスト）
export const AD_UNIT_IDS = {
	homeBanner: __DEV__ ? TestIds.ADAPTIVE_BANNER : 'ca-app-pub-XXXX/home-banner',
	gameExitInterstitial: __DEV__ ? TestIds.INTERSTITIAL : 'ca-app-pub-XXXX/game-exit',
	packUnlockRewarded: __DEV__ ? TestIds.REWARDED : 'ca-app-pub-XXXX/pack-unlock',
}
```

`src/lib/ads.ts`:

```ts
import {
	getTrackingPermissionsAsync,
	PermissionStatus,
	requestTrackingPermissionsAsync,
} from 'expo-tracking-transparency'
import mobileAds, { AdEventType, InterstitialAd } from 'react-native-google-mobile-ads'
import { AD_UNIT_IDS, INTERSTITIAL_EVERY_N_EXITS } from '@/constants/ads'
import { isPremiumUnlocked } from './premium'

// プレミアム購読者には一切広告を出さない。判定源は isPremiumUnlocked() のみ
export function adsEnabled(): boolean {
	return !isPremiumUnlocked()
}

let initialized = false
let interstitial: InterstitialAd | null = null
let interstitialLoaded = false
let playExitCount = 0

// 初回起動時（ホーム表示直後）に1回だけ呼ぶ。ATT → AdMob 初期化 → 先読み
export async function initAds(): Promise<void> {
	if (initialized || !adsEnabled()) return
	initialized = true
	const { status } = await getTrackingPermissionsAsync()
	if (status === PermissionStatus.UNDETERMINED) {
		await requestTrackingPermissionsAsync()
	}
	await mobileAds().initialize()
	preloadInterstitial()
}

function preloadInterstitial() {
	interstitialLoaded = false
	interstitial = InterstitialAd.createForAdRequest(AD_UNIT_IDS.gameExitInterstitial)
	interstitial.addAdEventListener(AdEventType.LOADED, () => {
		interstitialLoaded = true
	})
	// 表示後は次の退出に備えて再読込
	interstitial.addAdEventListener(AdEventType.CLOSED, preloadInterstitial)
	interstitial.load()
}

// GameScreen の play ステージ退出時に呼ぶ。N 回ごとに表示（初期値 1 = 毎回）
export function maybeShowGameExitInterstitial(): void {
	if (!initialized || !adsEnabled()) return
	playExitCount += 1
	if (playExitCount % INTERSTITIAL_EVERY_N_EXITS !== 0) return
	if (interstitialLoaded && interstitial) {
		interstitialLoaded = false
		interstitial.show()
	}
}

// テスト用: モジュール状態を初期化
export function _resetForTest() {
	initialized = false
	interstitial = null
	interstitialLoaded = false
	playExitCount = 0
}
```

- [ ] **Step 4: GREEN を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/lib/__tests__/ads.test.ts`
Expected: PASS（5テスト）

- [ ] **Step 5: Commit**

```bash
git add src/constants/ads.ts src/lib/ads.ts src/lib/__tests__/ads.test.ts
git commit -m "feat(#6): 広告初期化（ATT→AdMob）とプレミアム分岐を実装"
```

---

### Task 3: 退出インタースティシャルの表示ロジック

**Files:**

- Modify: `src/lib/ads.ts`（Task 2 で実装済みの `maybeShowGameExitInterstitial` の挙動をテストで固める。テストが通らない不足があればここで直す）
- Test: `src/lib/__tests__/ads.test.ts`（追記）
- Test: `src/lib/__tests__/ads-frequency.test.ts`（新規・N=3 のケース）

**Interfaces:**

- Consumes: Task 2 の `initAds` / `maybeShowGameExitInterstitial` / モックの `_interstitials`・`_emit`
- Produces: 挙動保証された `maybeShowGameExitInterstitial()`（Task 4 が GameScreen から呼ぶ）

- [ ] **Step 1: 失敗するテストを書く（ads.test.ts に追記）**

```ts
describe('maybeShowGameExitInterstitial', () => {
	it('ロード済みなら退出ごとに表示し、表示後に再読込する', async () => {
		await initAds()
		const first = adsMock._interstitials[0]
		first._emit('loaded')
		maybeShowGameExitInterstitial()
		expect(first.show).toHaveBeenCalledTimes(1)
		first._emit('closed')
		expect(adsMock.InterstitialAd.createForAdRequest).toHaveBeenCalledTimes(2)
		expect(adsMock._interstitials[1]?.load).toHaveBeenCalledTimes(1)
	})

	it('未ロードなら表示しない（クラッシュもしない）', async () => {
		await initAds()
		maybeShowGameExitInterstitial()
		expect(adsMock._interstitials[0]?.show).not.toHaveBeenCalled()
	})

	it('プレミアムなら何もしない', async () => {
		await initAds()
		adsMock._interstitials[0]._emit('loaded')
		mockedPremium.mockReturnValue(true)
		maybeShowGameExitInterstitial()
		expect(adsMock._interstitials[0].show).not.toHaveBeenCalled()
	})

	it('初期化前に呼ばれても何もしない', () => {
		expect(() => maybeShowGameExitInterstitial()).not.toThrow()
	})
})
```

（`maybeShowGameExitInterstitial` を import 行に追加すること）

- [ ] **Step 2: N=3 の頻度テストを別ファイルで書く**

`src/lib/__tests__/ads-frequency.test.ts`:

```ts
import type * as AdsMock from '../../../__mocks__/react-native-google-mobile-ads'
import { initAds, maybeShowGameExitInterstitial, _resetForTest } from '@/lib/ads'

jest.mock('@/lib/premium', () => ({ isPremiumUnlocked: jest.fn(() => false) }))
jest.mock('@/constants/ads', () => ({
	...jest.requireActual('@/constants/ads'),
	INTERSTITIAL_EVERY_N_EXITS: 3,
}))

const adsMock = jest.requireMock('react-native-google-mobile-ads') as typeof AdsMock

beforeEach(() => {
	jest.clearAllMocks()
	adsMock._resetAdsMock()
	_resetForTest()
})

it('N=3 のとき 3 回目の退出でだけ表示する', async () => {
	await initAds()
	const ad = adsMock._interstitials[0]
	ad._emit('loaded')
	maybeShowGameExitInterstitial()
	maybeShowGameExitInterstitial()
	expect(ad.show).not.toHaveBeenCalled()
	maybeShowGameExitInterstitial()
	expect(ad.show).toHaveBeenCalledTimes(1)
})
```

- [ ] **Step 3: RED/GREEN を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/lib/__tests__`
Expected: Task 2 実装で大半は既にパスするはず。落ちるテストがあれば `src/lib/ads.ts` を修正して PASS にする（テスト側は変えない）

- [ ] **Step 4: Commit**

```bash
git add src/lib/ads.ts src/lib/__tests__
git commit -m "test(#6): 退出インタースティシャルの頻度・再読込・プレミアム分岐を担保"
```

---

### Task 4: GameScreen への配線（play 退出のみカウント）

**Files:**

- Modify: `src/components/game/game-screen.tsx`
- Test: `src/components/game/__tests__/game-screen.test.tsx`（既存ファイルに追記。存在しなければ新規作成）

**Interfaces:**

- Consumes: `maybeShowGameExitInterstitial()`（@/lib/ads）
- Produces: なし（末端の配線）

- [ ] **Step 1: 失敗するテストを書く**

既存の game-screen テストのモック群に合わせて追加する（`@/lib/ads` はモック）:

```tsx
import { fireEvent, render } from '@testing-library/react-native'
import { GameScreen } from '@/components/game/game-screen'
import { maybeShowGameExitInterstitial } from '@/lib/ads'
import type { GameMeta } from '@/games/registry'

jest.mock('@/lib/ads', () => ({ maybeShowGameExitInterstitial: jest.fn() }))
jest.mock('expo-router', () => ({ router: { back: jest.fn() } }))

const meta: GameMeta = {
	id: 'test-game',
	title: 'テストゲーム',
	tagline: 'テスト用',
	emoji: '🎮',
	gradient: ['#000000', '#ffffff'],
	minPlayers: 1,
	maxPlayers: 8,
	howToPlay: ['あそぶ'],
	Component: () => null,
}

describe('GameScreen の退出広告', () => {
	it('イントロで閉じたときはインタースティシャルを呼ばない', async () => {
		const { getByLabelText } = await render(<GameScreen meta={meta} />)
		fireEvent.press(getByLabelText('閉じる'))
		expect(maybeShowGameExitInterstitial).not.toHaveBeenCalled()
	})

	it('play ステージからの戻るでインタースティシャルを1回呼ぶ', async () => {
		const { getByText } = await render(<GameScreen meta={meta} />)
		fireEvent.press(getByText('ゲームスタート'))
		fireEvent.press(getByText('‹'))
		expect(maybeShowGameExitInterstitial).toHaveBeenCalledTimes(1)
	})
})
```

注意: イントロの閉じるボタンの取得方法（accessibilityLabel の有無）は `game-intro-screen.tsx` の実装に合わせて調整してよいが、検証内容（呼ばれない/1回呼ばれる）は変えない。

- [ ] **Step 2: RED を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/components/game/__tests__/game-screen.test.tsx`
Expected: FAIL（play 退出テストで `maybeShowGameExitInterstitial` が 0 回）

- [ ] **Step 3: 実装（game-screen.tsx の play ステージ戻るボタンのみ変更）**

```tsx
import { maybeShowGameExitInterstitial } from '@/lib/ads'
```

play ステージのヘッダー戻る `onPress` を:

```tsx
onPress={() => {
	haptics.tap()
	maybeShowGameExitInterstitial()
	router.back()
}}
```

（イントロ画面の `onClose={() => router.back()}` は変更しない）

- [ ] **Step 4: GREEN を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/components/game`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/components/game
git commit -m "feat(#6): play ステージ退出時にインタースティシャルを表示"
```

---

### Task 5: ホーム下部バナー

**Files:**

- Create: `src/components/home/home-banner-ad.tsx`
- Modify: `src/app/index.tsx`
- Test: `src/components/home/__tests__/home-banner-ad.test.tsx`

**Interfaces:**

- Consumes: `adsEnabled()`（@/lib/ads）、`AD_UNIT_IDS.homeBanner`
- Produces: `HomeBannerAd`（props なし）

- [ ] **Step 1: 失敗するテストを書く**

`src/components/home/__tests__/home-banner-ad.test.tsx`:

```tsx
import { render } from '@testing-library/react-native'
import { HomeBannerAd } from '@/components/home/home-banner-ad'
import { adsEnabled } from '@/lib/ads'

jest.mock('react-native-google-mobile-ads')
jest.mock('@/lib/ads', () => ({ adsEnabled: jest.fn(() => true) }))
jest.mock('react-native-safe-area-context', () => ({
	useSafeAreaInsets: () => ({ top: 0, bottom: 0, left: 0, right: 0 }),
}))

const mockedEnabled = jest.mocked(adsEnabled)

describe('HomeBannerAd', () => {
	it('広告有効ならバナー枠を表示する', async () => {
		const { getByTestId } = await render(<HomeBannerAd />)
		expect(getByTestId('home-banner-ad')).toBeTruthy()
	})

	it('プレミアム（広告無効）なら何も描画しない', async () => {
		mockedEnabled.mockReturnValue(false)
		const { queryByTestId } = await render(<HomeBannerAd />)
		expect(queryByTestId('home-banner-ad')).toBeNull()
	})
})
```

- [ ] **Step 2: RED を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/components/home/__tests__/home-banner-ad.test.tsx`
Expected: FAIL（コンポーネント未作成）

- [ ] **Step 3: 実装**

`src/components/home/home-banner-ad.tsx`:

```tsx
import { StyleSheet, View } from 'react-native'
import { useSafeAreaInsets } from 'react-native-safe-area-context'
import { BannerAd, BannerAdSize } from 'react-native-google-mobile-ads'
import { AD_UNIT_IDS } from '@/constants/ads'
import { adsEnabled } from '@/lib/ads'

// ホーム下部の常設バナー。プレミアムは非表示（issue #6）
export function HomeBannerAd() {
	const insets = useSafeAreaInsets()
	if (!adsEnabled()) return null
	return (
		<View testID="home-banner-ad" style={[styles.wrap, { paddingBottom: insets.bottom }]}>
			<BannerAd
				unitId={AD_UNIT_IDS.homeBanner}
				size={BannerAdSize.ANCHORED_ADAPTIVE_BANNER}
			/>
		</View>
	)
}

const styles = StyleSheet.create({
	wrap: { alignItems: 'center' },
})
```

`src/app/index.tsx` — `</ScrollView>` の直後（screen View 内・スクロール外）に追加:

```tsx
import { HomeBannerAd } from '@/components/home/home-banner-ad'
```

```tsx
			</ScrollView>
			<HomeBannerAd />
		</View>
```

- [ ] **Step 4: GREEN＋ホーム既存テストの確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/components/home src/app`
Expected: PASS（index を描画する既存テストが SDK に触れても Task 1 の自動モックで落ちない）

- [ ] **Step 5: Commit**

```bash
git add src/components/home/home-banner-ad.tsx src/components/home/__tests__/home-banner-ad.test.tsx src/app/index.tsx
git commit -m "feat(#6): ホーム下部にアンカーバナーを常設（プレミアム非表示）"
```

---

### Task 6: 起動時の初期化配線（_layout）

**Files:**

- Modify: `src/app/_layout.tsx`
- Test: `src/app/__tests__/layout.test.tsx`（既存に追記）

**Interfaces:**

- Consumes: `initAds()`（@/lib/ads）
- Produces: なし（末端の配線）

- [ ] **Step 1: 失敗するテストを書く（layout.test.tsx に追記）**

既存のモック群に `jest.mock('@/lib/ads', () => ({ initAds: jest.fn() }))` を追加し:

```tsx
it('起動時に広告を初期化する', async () => {
	await render(<RootLayout />)
	expect(initAds).toHaveBeenCalledTimes(1)
})
```

- [ ] **Step 2: RED を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/app/__tests__/layout.test.tsx`
Expected: FAIL（initAds が 0 回）

- [ ] **Step 3: 実装（\_layout.tsx の useEffect 冒頭に追加）**

```tsx
import { initAds } from '@/lib/ads'
```

```tsx
	useEffect(() => {
		initAds()
		settingsStore.hydrate()
		...
```

- [ ] **Step 4: GREEN を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/app/__tests__/layout.test.tsx`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/app/_layout.tsx src/app/__tests__/layout.test.tsx
git commit -m "feat(#6): 起動時（ホーム表示直後）に ATT→AdMob 初期化を実行"
```

---

### Task 7: セッション解放ストアとプレミアムお題の取得経路

**Files:**

- Create: `src/lib/pack-unlock-store.ts`
- Modify: `src/lib/topics-store.ts`（`refreshPremiumPack` を追加）
- Test: `src/lib/__tests__/pack-unlock-store.test.ts`
- Test: `src/lib/__tests__/topics-store.test.ts`（既存に追記）

**Interfaces:**

- Consumes: `isPremiumUnlocked()`、`topicsStore` の既存 state/emit/CACHE_KEY
- Produces: `packUnlockStore.unlock(pack) / .lock(pack) / ._resetForTest()`、`isPackUnlocked(pack): boolean`、`usePackUnlocked(pack): boolean`、`topicsStore.refreshPremiumPack(pack): Promise<boolean>`

- [ ] **Step 1: 失敗するテストを書く**

`src/lib/__tests__/pack-unlock-store.test.ts`:

```ts
import { isPackUnlocked, packUnlockStore } from '@/lib/pack-unlock-store'
import { isPremiumUnlocked } from '@/lib/premium'

jest.mock('@/lib/premium', () => ({ isPremiumUnlocked: jest.fn(() => false) }))

const mockedPremium = jest.mocked(isPremiumUnlocked)

beforeEach(() => {
	jest.clearAllMocks()
	mockedPremium.mockReturnValue(false)
	packUnlockStore._resetForTest()
})

describe('packUnlockStore', () => {
	it('unlock で解放され、lock で再ロックされる', () => {
		expect(isPackUnlocked('king_premium')).toBe(false)
		packUnlockStore.unlock('king_premium')
		expect(isPackUnlocked('king_premium')).toBe(true)
		packUnlockStore.lock('king_premium')
		expect(isPackUnlocked('king_premium')).toBe(false)
	})

	it('プレミアムなら常に解放済み扱い', () => {
		mockedPremium.mockReturnValue(true)
		expect(isPackUnlocked('king_premium')).toBe(true)
	})

	it('解放は対象パックのみに効く', () => {
		packUnlockStore.unlock('king_premium')
		expect(isPackUnlocked('other_pack')).toBe(false)
	})

	it('unlock/lock で購読者に通知される', () => {
		const listener = jest.fn()
		packUnlockStore.subscribe(listener)
		packUnlockStore.unlock('king_premium')
		packUnlockStore.lock('king_premium')
		expect(listener).toHaveBeenCalledTimes(2)
	})
})
```

`topics-store.test.ts` への追記（既存の fetch モック手法に合わせる）:

```ts
describe('refreshPremiumPack', () => {
	it('取得したお題を重複なくマージする', async () => {
		// 既存 refresh で id: 'a' (pack 'king') が入っている状態を作ったうえで
		// fetch を [{ id: 'a', ... }, { id: 'p1', pack: 'king_premium', text: '壁ドン' }] を返すようにモック
		const ok = await topicsStore.refreshPremiumPack('king_premium')
		expect(ok).toBe(true)
		const packs = topicsStore.getState().topics.map((t) => t.id)
		expect(packs.filter((id) => id === 'a')).toHaveLength(1)
		expect(packs).toContain('p1')
	})

	it('取得失敗時は false を返し state を壊さない', async () => {
		// fetch を reject にモック
		const before = topicsStore.getState()
		const ok = await topicsStore.refreshPremiumPack('king_premium')
		expect(ok).toBe(false)
		expect(topicsStore.getState()).toEqual(before)
	})
})
```

（fetch のモック記述は既存 topics-store.test.ts の書き方に厳密に合わせること。検証内容は変えない）

- [ ] **Step 2: RED を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/lib/__tests__/pack-unlock-store.test.ts src/lib/__tests__/topics-store.test.ts`
Expected: FAIL（store 未作成 / refreshPremiumPack 未定義）

- [ ] **Step 3: 実装**

`src/lib/pack-unlock-store.ts`:

```ts
import { useSyncExternalStore } from 'react'
import { isPremiumUnlocked } from './premium'

// リワード視聴によるお題パックのセッション解放（メモリのみ・ゲーム退出で lock）。
// 永続化しないのは仕様: 「その飲み会のあいだ」だけ解放し、繰り返し視聴を促す（issue #6）
let unlockedPacks: ReadonlySet<string> = new Set()
const listeners = new Set<() => void>()

function emit() {
	listeners.forEach((fn) => fn())
}

export const packUnlockStore = {
	subscribe(fn: () => void): () => void {
		listeners.add(fn)
		return () => listeners.delete(fn)
	},
	unlock(pack: string) {
		unlockedPacks = new Set(unlockedPacks).add(pack)
		emit()
	},
	lock(pack: string) {
		const next = new Set(unlockedPacks)
		next.delete(pack)
		unlockedPacks = next
		emit()
	},
	_resetForTest() {
		unlockedPacks = new Set()
	},
}

export function isPackUnlocked(pack: string): boolean {
	return isPremiumUnlocked() || unlockedPacks.has(pack)
}

export function usePackUnlocked(pack: string): boolean {
	return useSyncExternalStore(
		packUnlockStore.subscribe,
		() => isPackUnlocked(pack),
		() => isPackUnlocked(pack),
	)
}
```

`src/lib/topics-store.ts` — `refresh()` の後に追加:

```ts
	// プレミアムお題パックの取得（リワード解放時・プレミアム時に呼ぶ）。
	// 通常の refresh は is_premium=false のみなので、ここで対象パックだけ追加取得してマージする
	async refreshPremiumPack(pack: string): Promise<boolean> {
		try {
			const anonKey = getAnonKey()
			const supabaseUrl = getSupabaseUrl()
			const res = await fetch(
				`${supabaseUrl}/rest/v1/topics?select=id,pack,text&pack=eq.${pack}&limit=1000`,
				{
					headers: {
						apikey: anonKey,
						Authorization: `Bearer ${anonKey}`,
					},
				},
			)
			if (!res.ok) return false
			const fetched = (await res.json()) as Topic[]
			const known = new Set(state.topics.map((t) => t.id))
			state = { ...state, topics: [...state.topics, ...fetched.filter((t) => !known.has(t.id))] }
			emit()
			await AsyncStorage.setItem(CACHE_KEY, JSON.stringify(state))
			return true
		} catch {
			return false
		}
	},
```

- [ ] **Step 4: GREEN を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/lib/__tests__`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/lib/pack-unlock-store.ts src/lib/topics-store.ts src/lib/__tests__
git commit -m "feat(#6): パックのセッション解放ストアとプレミアムお題取得経路を追加"
```

---

### Task 8: リワード結線（PremiumPackModal・王様ゲーム統合）

**Files:**

- Modify: `src/games/no-king-game/engine.ts`（定数 `KING_PREMIUM_PACK` を追加）
- Modify: `src/games/no-king-game/premium-pack-modal.tsx`（スタブ → 実装）
- Modify: `src/games/no-king-game/no-king-game.tsx`（お題プール拡張＋退出時 lock）
- Test: `src/games/no-king-game/__tests__/premium-pack-modal.test.tsx`（既存に追記・書き換え）
- Test: `src/games/no-king-game/__tests__/no-king-game.test.tsx`（既存に追記）

**Interfaces:**

- Consumes: `usePackUnlocked` / `packUnlockStore`（Task 7）、`useRewardedAd`（SDK）、`topicsStore.refreshPremiumPack`（Task 7）、`AD_UNIT_IDS.packUnlockRewarded`（Task 2）
- Produces: `KING_PREMIUM_PACK = 'king_premium'`（engine.ts）

- [ ] **Step 1: 失敗するテストを書く**

`premium-pack-modal.test.tsx`（既存の「近日対応予定」検証は新仕様に書き換え）:

```tsx
import { fireEvent, render } from '@testing-library/react-native'
import { useRewardedAd } from 'react-native-google-mobile-ads'
import { PremiumPackModal } from '@/games/no-king-game/premium-pack-modal'
import { packUnlockStore, usePackUnlocked } from '@/lib/pack-unlock-store'
import { topicsStore } from '@/lib/topics-store'

jest.mock('@/lib/premium', () => ({ isPremiumUnlocked: jest.fn(() => false) }))
jest.mock('@/lib/topics-store', () => ({
	topicsStore: { refreshPremiumPack: jest.fn(async () => true) },
}))

const mockedHook = jest.mocked(useRewardedAd)

function hookState(over: Partial<ReturnType<typeof useRewardedAd>>) {
	return {
		isLoaded: false,
		isEarnedReward: false,
		error: undefined,
		load: jest.fn(),
		show: jest.fn(),
		...over,
	} as ReturnType<typeof useRewardedAd>
}

beforeEach(() => {
	jest.clearAllMocks()
	packUnlockStore._resetForTest()
	mockedHook.mockReturnValue(hookState({}))
})

describe('PremiumPackModal', () => {
	it('表示時にリワードを読み込み、未ロード中はボタンが無効', async () => {
		const state = hookState({})
		mockedHook.mockReturnValue(state)
		const { getByText } = await render(<PremiumPackModal visible onClose={() => {}} />)
		expect(state.load).toHaveBeenCalled()
		expect(getByText('動画を見て解放する').props.disabled ?? true).toBeTruthy()
	})

	it('ロード済みならタップで動画を表示する', async () => {
		const state = hookState({ isLoaded: true })
		mockedHook.mockReturnValue(state)
		const { getByText } = await render(<PremiumPackModal visible onClose={() => {}} />)
		fireEvent.press(getByText('動画を見て解放する'))
		expect(state.show).toHaveBeenCalledTimes(1)
	})

	it('視聴完了でパックを解放しプレミアムお題を取得する', async () => {
		mockedHook.mockReturnValue(hookState({ isEarnedReward: true }))
		await render(<PremiumPackModal visible onClose={() => {}} />)
		expect(usePackUnlockedValue()).toBe(true)
		expect(topicsStore.refreshPremiumPack).toHaveBeenCalledWith('king_premium')
	})

	it('解放済みなら解放済み表示になり動画ボタンを出さない', async () => {
		packUnlockStore.unlock('king_premium')
		const { queryByText, getByText } = await render(
			<PremiumPackModal visible onClose={() => {}} />,
		)
		expect(queryByText('動画を見て解放する')).toBeNull()
		expect(getByText(/解放中/)).toBeTruthy()
	})
})

function usePackUnlockedValue(): boolean {
	let value = false
	function Probe() {
		value = usePackUnlocked('king_premium')
		return null
	}
	render(<Probe />)
	return value
}
```

（`usePackUnlockedValue` の render が非同期規約に合わない場合は `isPackUnlocked('king_premium')` の直接検証に置き換えてよい。検証内容は変えない）

`no-king-game.test.tsx` への追記:

```tsx
it('king_premium 解放中はプレミアムお題もプールに入り、アンマウントで再ロックされる', async () => {
	packUnlockStore.unlock('king_premium')
	const { unmount } = await render(<NoKingGame />)
	unmount()
	expect(isPackUnlocked('king_premium')).toBe(false)
})
```

（お題プール拡張の直接検証が既存テスト構造で難しければ、`kingTopics` 相当のフィルタを純関数 `collectKingTopics(topics, premiumUnlocked)` として engine.ts に切り出し、それを単体テストする形にしてよい）

- [ ] **Step 2: RED を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/games/no-king-game`
Expected: FAIL（モーダルはスタブのまま・lock 未実装）

- [ ] **Step 3: 実装**

`src/games/no-king-game/engine.ts` に追加:

```ts
// リワード解放対象の限定お題パック（Supabase seed 済み・is_premium=true）
export const KING_PREMIUM_PACK = 'king_premium'
```

`src/games/no-king-game/premium-pack-modal.tsx` を全面置き換え:

```tsx
import { MaterialCommunityIcons } from '@expo/vector-icons'
import { useEffect } from 'react'
import { Modal, Pressable, StyleSheet, Text, View } from 'react-native'
import { useRewardedAd } from 'react-native-google-mobile-ads'
import { GradientButton } from '@/components/ui/gradient-button'
import { AD_UNIT_IDS } from '@/constants/ads'
import { packUnlockStore, usePackUnlocked } from '@/lib/pack-unlock-store'
import { topicsStore } from '@/lib/topics-store'
import { colors, radii, spacing, typography } from '@/theme/tokens'
import { KING_PREMIUM_PACK } from './engine'

type Props = {
	visible: boolean
	onClose: () => void
}

// 限定お題パック（king_premium）の解放モーダル。
// 動画リワード視聴でこのゲームセッション中のみ解放（退出で再ロック）。プレミアムは常時解放
export function PremiumPackModal({ visible, onClose }: Props) {
	const unlocked = usePackUnlocked(KING_PREMIUM_PACK)
	const { isLoaded, isEarnedReward, load, show } = useRewardedAd(AD_UNIT_IDS.packUnlockRewarded)

	useEffect(() => {
		if (visible && !unlocked) load()
	}, [visible, unlocked, load])

	useEffect(() => {
		if (isEarnedReward) {
			packUnlockStore.unlock(KING_PREMIUM_PACK)
			topicsStore.refreshPremiumPack(KING_PREMIUM_PACK)
		}
	}, [isEarnedReward])

	return (
		<Modal visible={visible} transparent animationType="fade" onRequestClose={onClose}>
			<Pressable style={styles.backdrop} onPress={onClose}>
				<Pressable style={styles.sheet} onPress={() => {}}>
					<MaterialCommunityIcons
						name={unlocked ? 'lock-open-variant' : 'lock'}
						testID={unlocked ? 'icon-lock-open' : 'icon-lock'}
						size={48}
						color={colors.premiumGold}
					/>
					<Text style={styles.title}>限定お題パック</Text>
					{unlocked ? (
						<Text style={styles.desc}>
							このセッション中は解放中！{'\n'}
							ドキドキ度アップの限定お題が混ざります。
						</Text>
					) : (
						<>
							<Text style={styles.desc}>
								ドキドキ度アップの限定お題が遊べるパックです。{'\n'}
								動画を見るとこのセッション中だけ解放されます。
							</Text>
							<GradientButton
								title="動画を見て解放する"
								disabled={!isLoaded}
								onPress={() => show()}
							/>
						</>
					)}
					<Pressable accessibilityRole="button" onPress={onClose}>
						<Text style={styles.close}>とじる</Text>
					</Pressable>
				</Pressable>
			</Pressable>
		</Modal>
	)
}

const styles = StyleSheet.create({
	backdrop: {
		flex: 1,
		backgroundColor: 'rgba(0,0,0,0.6)',
		alignItems: 'center',
		justifyContent: 'center',
		padding: spacing.lg,
	},
	sheet: {
		alignSelf: 'stretch',
		backgroundColor: colors.surface,
		borderRadius: radii.lg,
		borderWidth: 1,
		borderColor: colors.surfaceBorder,
		padding: spacing.lg,
		alignItems: 'center',
		gap: spacing.md,
	},
	title: { ...typography.title },
	desc: { ...typography.body, color: colors.textMuted, textAlign: 'center', lineHeight: 24 },
	close: { ...typography.body, color: colors.textMuted, padding: spacing.sm },
})
```

（`GradientButton` に `disabled` プロップが無い場合は、無効時に `onPress` を no-op にする最小拡張を `GradientButton` に加え、既存テストが壊れないことを確認する）

`src/games/no-king-game/no-king-game.tsx`:

```tsx
import { useEffect } from 'react'
import { packUnlockStore, usePackUnlocked } from '@/lib/pack-unlock-store'
import { KING_PREMIUM_PACK } from './engine'
```

```tsx
const premiumUnlocked = usePackUnlocked(KING_PREMIUM_PACK)
const kingTopics = topics.filter(
	(t) => t.pack === 'king' || (premiumUnlocked && t.pack === KING_PREMIUM_PACK),
)

// セッション解放は「王様ゲームから出るまで」。アンマウントで再ロック（issue #6）
useEffect(() => () => packUnlockStore.lock(KING_PREMIUM_PACK), [])
```

- [ ] **Step 4: GREEN を確認**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace src/games/no-king-game`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/games/no-king-game src/lib src/components/ui
git commit -m "feat(#6): 王様ゲーム限定パックのリワード解放を実装（セッション中のみ）"
```

---

### Task 9: 全体検証と仕上げ

**Files:**

- Modify: なし（検証のみ。問題があれば該当タスクに戻って修正）

- [ ] **Step 1: 全テスト実行**

Run: `node ./node_modules/jest-cli/bin/jest.js --runInBand --silent --noStackTrace`
Expected: 全スイートパス

- [ ] **Step 2: 型チェックと lint（今回変更したファイルのみ）**

Run: `npx tsc --noEmit`
Expected: エラーなし

Run: `npx eslint src/lib/ads.ts src/lib/pack-unlock-store.ts src/lib/topics-store.ts src/constants/ads.ts src/components/home/home-banner-ad.tsx src/components/game/game-screen.tsx src/app/index.tsx src/app/_layout.tsx src/games/no-king-game --max-warnings 0`
Expected: エラー・警告なし

- [ ] **Step 3: Prettier 確認**

Run: `npx prettier --check "src/**/*.{ts,tsx}" app.json "__mocks__/**"`
Expected: All matched files use Prettier code style!

- [ ] **Step 4: PR 作成**

PR 本文に含めること: 配置決定の要約（設計書リンク）/ テスト結果 / 実機確認の残タスク:

- [ ] dev build（EAS）でテスト広告の表示確認（バナー・退出インタースティシャル・リワード）
    - 注意: 現状 `isPremiumUnlocked()` は `__DEV__` で true を返すため、dev build では広告が出ない。確認時は `src/lib/premium.ts` を一時的に `return false` にする（コミットしない）。恒久対応は #7
- [ ] iOS 実機で ATT ダイアログの初回表示確認
- [ ] リリース前: `src/constants/ads.ts` の本番ユニット ID・`app.json` の本番 App ID 差し替え
