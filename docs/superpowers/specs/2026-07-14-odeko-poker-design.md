# おでこインディアンポーカー（G12 / #62・プレミアム）設計

2026-07-14 確定。ブレインストーミングでの決定事項と設計をまとめる。

## 決定事項

- **勝負者が1人だけの場合は負けなし**: 度胸を見せた勝負者の一人勝ち表示。負け（飲む人）はヘタレ賞のみ発生しうる
- **ヘタレ賞は「その回の最強カードで降りた人」**: 配られたカードの中で一番強いカードを持っていたのに降りた場合のみ。最大1人。全員降り（全員負け）のときもバッジ表示して笑いに変える
- **1ラウンド完結＋連続プレイ**: スコア累計なし。結果発表後「次のラウンド」で即再配布（no-king-game 方式）
- **宣言は秘密**: 長押しで確定した瞬間に中立画面へ切り替え、結果発表まで全員の宣言を伏せる（issue 指定の長押し確認パターン）
- **プレミアム限定ゲーム（全体ロック）**: 確立済みの共通ゲート（`GameMeta.premium` ＋ premium-lock-modal）をそのまま流用。新規実装なし
- **実装アプローチ**: 純粋エンジン＋reducer 方式（daut-dice / sasayaki-limit 準拠）
- 参加 3〜12人・`requiresPlayers: true`（カード公開・敗者発表に名前とプレイヤーカラーが必要）

## ルール確定版

- 各プレイヤーに 1〜13 のカードを重複なしで1枚ずつ配る。**13 が最強、1 が最弱**
- **確認フェーズ**: 順番にスマホを額に当てて掲げる（画面は外向き、本人は見えない）。他の全員はその人のカードを覚える
- **宣言フェーズ**: 順番に「勝負」か「降りる」を長押しで秘密宣言
- **結果発表**: ドラムロール → 全カード＋全宣言を一斉公開 → 判定
    - 勝負者 2人以上 → 勝負者の中で最弱カードの人が負け
    - 勝負者 1人 → 負けなし（勝負者の一人勝ち）
    - 全員降り → 全員負け
    - ヘタレ賞: その回の最強カード保持者が降りていたら、負けと一緒に飲む

## ゲームフロー（フェーズ）

```
intro（遊び方・プレミアムゲートは registry 側）
 → deal（ラウンド開始。「カードを配りました」→ 確認フェーズへ）
 → forehead × player p (1..n):（1台回し）
     handoff（「◯◯さんにスマホを渡して」）
      → 本人がタップ → 覗き見防止カウントダウン3秒（画面を外向きにして額に当てる猶予）
      → カード大表示5秒（残り秒数表示）→ 自動で次の人の handoff へ
 → declare × player p (1..n):（1台回し）
     handoff（「◯◯さんにスマホを渡して」）
      → 「勝負」/「降りる」の2ボタンを長押しで確定
      → 確定した瞬間に次の人の handoff（中立画面を兼ねる）へ切り替え
 → result（ドラムロール〔共通 drumroll-reveal 流用、result-screen 内〕→ 全カード＋宣言公開 → 敗者ドン／一人勝ち／全員負け＋ヘタレ賞バッジ）
     → 「次のラウンド」→ deal に戻り再配布 ／ 「終了」→ ホームへ
```

- 確認フェーズのタイマー（3秒＋5秒）は forehead-screen 内で完結し、終了時に reducer へ進行アクションを送る
- 宣言の長押しは 600ms（誤タップ防止）。確定後は戻れない

## 判定（純関数 engine.ts、境界値テスト付き）

- `dealCards(playerCount, rng)` → 1〜13 から重複なしで playerCount 枚（シャッフルの先頭 n 枚）
- `judge(cards, declarations)` → `{ outcome, loserIndices, hetareIndex }`
    - `outcome: 'normal' | 'solo-fight' | 'all-fold'`
    - `normal`（勝負者2人以上）: 勝負者中の最弱カードの index が `loserIndices`
    - `solo-fight`: `loserIndices = []`、勝者 index は宣言配列から導出
    - `all-fold`: `loserIndices = 全員`
    - `hetareIndex`: 全カード中の最大値保持者が `fold` なら その index、そうでなければ `null`（outcome に関係なく判定）
- 乱数は注入可能（`rng: () => number`）にしてテスト可能に

## ファイル構成（src/games/odeko-poker/）

| ファイル               | 役割                                                                     |
| ---------------------- | ------------------------------------------------------------------------ |
| `odeko-poker-game.tsx` | フェーズ分岐のルート（reducer 方式）                                     |
| `engine.ts`            | カード配布・勝敗判定・ヘタレ賞判定の純関数                               |
| `reducer.ts`           | フェーズ遷移（deal → forehead×n → declare×n → result → 再配布）          |
| `forehead-screen.tsx`  | 額当て確認UI（handoff → カウントダウン3秒 → カード大表示5秒 → 自動送り） |
| `declare-screen.tsx`   | 長押し秘密宣言UI（handoff → 長押し確定 → 次の人の handoff へ）           |
| `result-screen.tsx`    | ドラムロール → 全カード公開 → 敗者ドン／一人勝ち／全員負け・ヘタレ賞演出 |
| `theme.ts`             | 配色トークン                                                             |
| `__tests__/`           | engine・reducer・各画面のテスト                                          |

## registry 追加

- `id: 'odeko-poker'`、title「おでこインディアンポーカー」、emoji 🎴、gradient `['#F368E0', '#8854D0']`
- `minPlayers: 3, maxPlayers: 12, requiresPlayers: true, premium: true`
- 遊び方4行（額当て → 覚える → 秘密宣言 → 判定＋ヘタレ賞）、catchCopy / summary
- CLAUDE.md の収録ゲーム候補に追記

## テスト方針

- engine 境界テスト: 全員降り／勝負1人／勝負2人の最弱判定／ヘタレ賞あり・なし（最強が勝負していたら無し）／配布の重複なし・枚数・値域
- reducer: フェーズ遷移の全経路（forehead→declare の人数分ループ、再配布で状態リセット）
- タイマー系テストは React 19 の await act 規約（kimagure-ox 準拠）
- 乱数注入で決定的にテスト

## スコープ外

- スコア累計・戦績保存
- カードのスート表示（数字のみ。トランプ風の見た目は演出の範囲で）
- 複数端末同期
